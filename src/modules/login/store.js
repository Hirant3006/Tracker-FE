const state = {
	isLoading2 : false,
}

const getters = {
};

const actions = {
};

const mutations = {
};
export default {
    state,
    actions,
    mutations,
    getters,
    namespaced: true,
};